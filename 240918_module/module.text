# 파이썬 모듈의 정의와 구조
모듈은 관련된 함수, 클래스, 변수를 모아 놓은 파일(.py) 
제작한 모듈 파일(.py) 내에서 정의된 함수나 클래스는 다른 파일에서 import 문을 사용하여 코드에 적용

# 모듈 구조
파일명: 모듈의 이름은 파일명으로 결정 EX: my_module.py
함수 및 클래스 정의: 모듈 내에서 여러 함수와 클래스를 정의
__name__ 변수: 모듈이 직접 실행될 때와 다른 모듈에서 불려질 때의 동작을 구분하기 위해 사용