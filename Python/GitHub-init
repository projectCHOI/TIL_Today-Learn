git <코드 관리를 위한 git 명령어>
//파일 생성 후 cmd창에서 수행
//파일 저장 후 git에 업로드
0-1) git init
- git status
1-1) git add 파일명.py: 파일 수정 시 사용
1-2) git commit -m "메세지": git에 메세지 보냄
- git log
0-2) git remote add origin 레파지토리 주소 : 원격접속 허용
1-3) git push -u origin main: 서버에 업로드
       -> (n/n)완료 메세지 떠야 함
       -> 안될 시, git push 해보고 git push --set-upstream origin master 먼저 수행
<git branch 이동>
1) git checkout 'branch 주소'
2) git checkout master : master 의 가장 최신 파일로 이동
<git 공동작업 방법>
- (code창에서) git clone '주소'
- git pull
